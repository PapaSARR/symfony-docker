{% extends 'base.html.twig' %}

{% block title %}Contact{% endblock %}

{% block body %}
<div class="container">
    {% for message in app.flashes('success') %}
        <div class="alert alert-info mt-4">
            {{ message }}
        </div>
    {% endfor %}
    <h1 class="mt-4 text-center">Formulaire de contact</h1>
    <hr>
    {{ form_start(form) }}
    {{ form_widget(form) }}
    <input type="submit" value="Envoyer" class="btn btn-primary" formnovalidate/>
    {{ form_end(form) }}
</div>
{% endblock %}